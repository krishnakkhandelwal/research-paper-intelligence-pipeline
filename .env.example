# LLM provider config (used in Phase 2 enrichment)
LLM_PROVIDER=openai
OPENAI_API_KEY=
GEMINI_API_KEY=

# Storage (Phase 2)
DB_PATH=./data/papers.db
FAISS_INDEX_PATH=./data/faiss_index

# Embedding / chunking (Phase 2)
EMBEDDING_MODEL=all-MiniLM-L6-v2
CHUNK_SIZE=500
CHUNK_OVERLAP=50
TOP_K=5
